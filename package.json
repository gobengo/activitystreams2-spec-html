{
  "name": "activitystreams2-spec-html",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run tsc",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "lint-fix": "npm run fix",
    "lint": "npm run check",
    "make:lib": "npm run build",
    "posttest": "npm run check",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "preversion": "npm test",
    "start": "ts-node src",
    "test": "ts-node src/test",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/html-minifier": "^3.5.2",
    "activitystreams2-spec-scraped": "^0.10.0",
    "lit-ntml": "^1.3.1",
    "ts-node": "^7.0.1"
  },
  "devDependencies": {
    "typescript": "^3.0.3"
  }
}
